# .env.development

# This ensures local development still talks to your local backend server.
VITE_API_BASE_URL=http://localhost:5000/api 
# (Assuming your local server uses a common prefix like /api for its routes)